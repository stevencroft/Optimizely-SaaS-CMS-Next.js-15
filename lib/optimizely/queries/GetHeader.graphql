query getHeader($locale: [Locales]) {
  Header(locale: $locale) {
    items {
      logo
      ctaText
      ctaHref
      navItems {
        ... on NavItem {
          label
          href
        }
      }
    }
  }
}
